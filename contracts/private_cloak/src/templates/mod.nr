/// Cloak Template Contracts
///
/// Currently only Governor Bravo (Protocol Governance) is enabled.
/// Other templates are on hold pending compilation fixes.

pub mod governor_bravo_cloak;
// molt_cloak lives in its own package: contracts/molt_cloak/
// pub mod organization_cloak;   // On hold
// pub mod treasury_cloak;       // On hold
// pub mod grants_cloak;         // On hold
// pub mod workplace_cloak;      // On hold
